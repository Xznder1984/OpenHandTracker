{
  "name": "openhandtrack-virtual-piano",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OpenHandTrack example — play a piano with your hands, no keyboard required.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^5.4.0"
  }
}
