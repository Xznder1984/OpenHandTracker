{
  "name": "openhandtrack-web-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OpenHandTrack web demo — real-time hand tracking in the browser with @mediapipe/tasks-vision.",
  "license": "Apache-2.0",
  "keywords": [
    "hand-tracking",
    "mediapipe",
    "computer-vision",
    "gesture",
    "real-time"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "vite": "^5.4.0"
  }
}
